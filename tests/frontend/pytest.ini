[pytest]
# Frontend Dash UI tests — requires Chrome and dash[testing]
markers =
    frontend: Dash UI tests run against a headless Chrome browser

# Tell dash.testing where Chrome is
addopts = -p no:warnings
